"use client";

import React from "react";
import { Sparkles } from "lucide-react";
import WidgetContainer from "@/components/Ui/Widgets/WidgetContainer";
import InsightCard from "@/components/Ui/Cards/InsightCard";
import { MOCK_INSIGHTS } from "@/lib/mock-data/insights";

export default function AIInsightsWidget() {
  return (
    <WidgetContainer
      title="AI Financial Intelligence & Signals"
      subtitle="Automated fundamental analysis, growth signals & risk flags generated by AI"
      actionText="All Intelligence Reports"
      actionHref="/research"
      icon={<Sparkles size={20} className="text-[#FACC15]" />}
    >
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {MOCK_INSIGHTS.slice(0, 2).map((insight) => (
          <InsightCard key={insight.id} insight={insight} />
        ))}
      </div>
    </WidgetContainer>
  );
}
